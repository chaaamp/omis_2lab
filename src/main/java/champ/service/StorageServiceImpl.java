package champ.service;

import champ.model.Document;

public class StorageServiceImpl implements StorageService {
    public void saveToCloud(Document document) {
        // Сохранение документа в облаке
    }
    public void saveToLocal(Document document, String path) {
        // Локальное сохранение документа
    }
}